name: YAML Lint
on:
  push:
    paths: [ '**/*.yaml', '**/*.yml' ]
  pull_request:
    paths: [ '**/*.yaml', '**/*.yml' ]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate YAML
        uses: ibiqlik/action-yaml-lint@v3
